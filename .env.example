EXPO_PUBLIC_CONVEX_URL=https://deafening-turtle-227.convex.cloud
EXPO_PUBLIC_STRIPE_KEY=
EXPO_PUBLIC_SENTRY_DSN=

# Convex backend secrets (configure with `npx convex env set`, do not expose in Expo public env):
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# ALLOW_STRIPE_MOCK=false
# ALLOW_KYC_MOCK=false
# RESEND_API_KEY=re_xxxxxxxx

# Optional - support module (currently disabled in app)
EXPO_PUBLIC_ZENDESK_SUBDOMAIN=
EXPO_PUBLIC_ZENDESK_ENABLED=false

# Zendesk backend secrets (configure in Convex env, never in Expo public env):
# ZENDESK_ENABLED=false
# ZENDESK_SUBDOMAIN=
# ZENDESK_EMAIL=
# ZENDESK_API_TOKEN=
